# DaikuCalc-web 開発記録

## アプリ概要

大工計算アプリのWebバージョン（HTML/CSS/JS 単一ファイル）。
iOSアプリ（SwiftUI）から移植・改良。

---

## 画面構成

### タブ①：斜辺・角度
- 入力：水平 a、垂直 b
- 出力：斜辺（筋交い）、勾配
- SVG三角形に角A・角B・斜辺・勾配を表示

### タブ②：勾配・高さ
- 入力：勾配、水平 H
- 出力：屋根高さ、斜辺（垂木長さ）
- タブ①から勾配を引き継ぐ、またはここで手動入力可

---

## タブ間連動仕様

| 操作 | 期待する動作 |
|---|---|
| タブ①で a・b を入力 → タブ②へ | 勾配セルに自動計算値が表示される |
| タブ②で勾配を入力 → タブ①へ | a=1000mm として斜辺・角度を表示（②連動モード） |
| 勾配をすべて消す | `slopeFrom='none'` に戻る（タブ①で再入力で通常モードへ） |

---

## 修正履歴

### 2026-07-12 スマホ表示の根本修正（デスクトップ用サイト表示対応）

**問題**：ひさしさんのiPhone（ホーム画面Web版）はSafariの「デスクトップ用サイト表示」で
開かれるため、px固定寸法の全体が約4割サイズに縮小表示されていた（user-scalable=noで拡大も不可）。
逆に正しいモバイル表示にすると高さが足りず、電卓の1・2・3と0の段が画面外に切れて押せなかった。

**修正**：
- 文字・ボタン寸法をpx固定→rem基準に変換し、タッチ端末では `html{font-size:4.1vw}`
  （画面幅の4.1%）で決定。表示モードに関係なく実寸が同じになる（朝刊ニュースと同じ方式）
- 電卓キー高さを `min(7rem, 8.6dvh)` にして、どの画面高さでも4段すべて収まるように
- 下部スペーサーdivを廃止（三角形エリアflex:1が余白を吸収）
- 三角形SVGを `preserveAspectRatio="xMidYMid meet"` に変更（引き伸ばしで勾配の
  見た目が狂うのを防止。形が常に正しい）
- セル見出し縮小（1.375→1.25rem）で「斜辺（筋交い）」の折り返し解消

**検証**：375×812のモバイル表示で全要素が収まること、①1000×500→斜辺1118・勾配5.00寸、
②勾配連動＋H3500→屋根高さ1750・垂木3913 を実クリックで確認。

### 2026-05-08 タブ間勾配連動バグ修正（commit: 4dc209f）

**問題①：タブ②で勾配入力 → タブ①に切り替えても連動しない**

- 原因：`kp()` でslope2フィールドに入力しても `S.slopeFrom` が `'manual'` に更新されていなかった
- タブ①の `fs2` 判定は `S.slopeFrom==='manual'` を見るため、常にfalseになっていた
- 修正：slope2入力時に `S.slope = v` と `S.slopeFrom = 'manual'` を同期するよう変更

```js
// kp() 内に追加
if (f==='slope2') {
  S.slope = v;
  S.slopeFrom = v!=='' ? 'manual' : 'none';
}
```

**問題②：タブ①で計算した勾配 → タブ②に切り替えると表示されない・計算されない**

- 原因：`calc()` の `sl2` が `S.slope2`（空文字）のみ参照し、`S.slope`（タブ①の連動値）を無視していた
- セル表示も `S.slope2` だけ見ていたため「—」表示になっていた
- SVGは `S.slope` を表示していたため、見た目と計算結果が食い違っていた

```js
// calc() 修正
const sl2 = S.slope2!=='' ? parseFloat(S.slope2)||0 : sl;

// render() タブ② セル表示
const effSl = S.slope2 || (S.slopeFrom!=='none' ? S.slope : '');
sc('ca-v', effSl ? effSl+'寸' : '');

// render() タブ② SVG
const svgSl = S.slope2||S.slope;
st('tS', svgSl?'勾配 '+svgSl+'寸':'勾配 —', !!svgSl);
```

---

## 状態変数まとめ（S オブジェクト）

| 変数 | 意味 |
|---|---|
| `S.tab` | 現在のタブ（0=①、1=②） |
| `S.a` | タブ①：水平 a の入力値 |
| `S.b` | タブ①：垂直 b の入力値 |
| `S.slope` | 有効な勾配（自動計算 or 手動）|
| `S.slopeFrom` | `'none'` / `'screen1'`（①自動）/ `'manual'`（②手動） |
| `S.slope2` | タブ②で直接入力した勾配 |
| `S.H` | タブ②：水平 H の入力値 |
